# Agent 改进计划

## 一、功能性（影响实用性）

- [ ] 1. 支持取消正在进行的请求（`cancel()` / `abort()`）
- [ ] 2. 对话历史管理（上下文窗口限制、`clearHistory()`、历史截断策略）
- [ ] 3. 新增 `write_file` 工具
- [ ] 4. 新增 `list_dir` 工具
- [ ] 5. 支持从 JS 侧注册自定义工具
- [ ] 6. `AgentConfig` 支持 `temperature`、`max_tokens`、`top_p` 等模型参数

## 二、健壮性（影响可靠性）

- [ ] 7. Agent 循环添加最大轮次限制（防止无限循环）
- [ ] 8. 追踪 token 用量（解析 SSE 中的 usage 数据）
- [ ] 9. 优雅关闭机制（`close()` / `destroy()`）
- [ ] 10. shell 工具添加超时限制

## 三、API 设计（影响开发体验）

- [ ] 11. `sendMessage` 返回 Promise
- [ ] 12. 新增工具调用事件（onToolCall / onToolResult）
- [ ] 13. 错误信息结构化（错误码、错误类型）

## 四、工程性（影响项目成熟度）

- [ ] 14. GitHub Actions CI/CD（多平台构建）
- [ ] 15. 补充测试覆盖（agent 循环、工具执行、SSE 解析）
- [ ] 16. CHANGELOG
- [ ] 17. `.npmignore`（排除 src/、target/ 等）
